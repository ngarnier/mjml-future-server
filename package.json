{
  "name": "mj-giphy-server",
  "version": "1.0.0",
  "description": "A neat server that will return a GIF from a request",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nicolas Garnier",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.2",
    "request-promise": "^4.2.0"
  }
}
